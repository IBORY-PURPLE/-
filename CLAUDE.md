# 말길 (Malgil) — 프로젝트 규칙

## 문서 위계

| 문서                                                   | 역할                                                  |
| ------------------------------------------------------ | ----------------------------------------------------- |
| [Docs/말길\_기획서\_v0.8.md](Docs/말길_기획서_v0.8.md) | **현행 정본.** 왜 · 무엇을 · 어떤 정책으로 · 언제까지 |
| [Docs/PRD.md](Docs/PRD.md)                             | 기능별 상세 사양 · 수용 기준                          |
| [Docs/TSD.md](Docs/TSD.md)                             | 아키텍처 · API 실측 스펙 · 데이터 모델                |
| [Docs/plan/](Docs/plan/)                               | 데이터 활용 계획 · 실측 리포트                        |

세 문서가 충돌하면 **기획서 §4(서비스 정책 · 용어 정본)**가 우선합니다.

**출품처는 두 곳입니다** — 2026 관광데이터 활용 공모전(9/21 16:00) · 모두의 창업 로컬 트랙(9/17 16:00). 일정은 **더 이른 9/17을 먼저** 놓고 계산합니다.

---

## data/ 폴더 구조 ★필수

> **이 프로젝트의 본업은 데이터 분석입니다.** 무엇을 읽어야 하는지 헷갈리면 안 됩니다.
> **사람이 읽을 것과 AI가 읽을 것을 물리적으로 분리합니다.**

```
data/
├── AI/                          ← AI가 읽는 것. 사람은 안 읽어도 됨
│   ├── analyze_89_culture.py         분석 스크립트
│   ├── pull_visitor.py               수집 스크립트
│   ├── dump.py
│   └── _raw/                    ← 원본 · 중간 파일 (대용량 · 사람이 안 읽음)
│       ├── dump.json                 TourAPI 전량 덤프 (18MB · .gitignore)
│       ├── malgil_89_join.csv        행안부 89곳 + 인구
│       └── age_sgg_utf8.csv          주민등록 인구 원본
└── 인간/                         ← 사람이 읽고 이해할 결과물. 자기설명 블록 필수
    ├── analyze_89_culture.json
    └── analyze_visitor_gap.json
```

### 짝 이름 규칙

**산출물 파일명 = 그것을 만든 스크립트명.** 확장자만 다릅니다. 파일을 열었을 때 어느 스크립트가 만들었는지 이름만으로 알 수 있어야 합니다.

```
data/AI/analyze_89_culture.py   →   data/인간/analyze_89_culture.json
data/AI/analyze_visitor_gap.py  →   data/인간/analyze_visitor_gap.json
```

| 규칙 | 내용 |
|---|---|
| **1 스크립트 = 1 산출물** | 데이터셋이 여러 개면 **한 JSON 안에 섹션으로** 나눕니다. 파일을 쪼개지 않습니다 |
| **`analyze_*.py`만 짝을 만듭니다** | 분석 결과라야 사람이 읽을 가치가 있습니다 |
| **`pull_*.py` · `dump.py`는 `_raw/`에만 씁니다** | 수집 원본은 사람이 읽는 물건이 아닙니다. 짝을 만들지 않습니다 |
| **중간 산출물도 `_raw/`** | `regions.json` · `deep.json` 처럼 다른 스크립트의 입력으로만 쓰이는 것 |
| **`build_*.py`는 앱 자산을 만듭니다** | `data/AI/build_app_assets.py` → `app/flutter/assets/data/build_app_assets.json`. 짝 이름 · `_설명` 첫 키 · 섹션 규칙을 그대로 지킵니다. 앱이 읽는 파일이라고 예외가 아닙니다 |

### 앱 폴더 (`app/`)

저장소 전체가 ASCII 경로 **`C:\Users\chewo\StudioProjects\malgil\`** 에 있고, `app/`은 그 안의 일반 폴더입니다. Flutter·Node 도구가 한글 경로에서 깨지므로 **저장소를 한글 경로로 옮기지 않습니다**(2026-09-14까지는 한글 경로 저장소 + `app/` 정션 구조였고, 이날 한 곳으로 합쳤습니다).

- `app/flutter/` Flutter Web 앱 · `app/worker/` Cloudflare Workers 프록시(Hono)
- 브랜치마다 [Docs/개발일지.md](Docs/개발일지.md)에 항목을 남깁니다(무엇 · 왜 · 실행법 · 확인한 것 · 남은 것).

여러 데이터셋을 한 파일에 담는 형태는 이렇습니다.

```json
{
  "_설명": { "파일": "analyze_89_culture.json", "섹션": { "문화두께": {...}, "예비시드": {...} } },
  "데이터": { "문화두께": [ 89행 ], "예비시드": [ 89행 ] }
}
```

### 스크립트 경로 헤더 (모든 `data/AI/*.py` 공통)

```python
HERE = os.path.dirname(os.path.abspath(__file__))          # data/AI
RAW = os.path.join(HERE, "_raw")                          # 원본·중간 파일
OUT = os.path.join(os.path.dirname(HERE), "인간")          # 사람이 읽는 산출물
```

입력은 `RAW`, 사람이 읽을 산출물은 `OUT`에서만 읽고 씁니다. 경로를 문자열로 직접 쓰지 않습니다.

---

## 데이터 산출물 자기설명 규칙 ★필수

> **데이터를 분석해 JSON · CSV를 생성할 때마다 지킵니다.** 예외 없음.
> 목적 — 파일만 열어봐도 각 항목이 무엇인지 알 수 있어야 합니다. 사람에게 하나하나 물어보게 만들지 않습니다.

### 원칙

산출 파일 **맨 앞**에 한국어 설명 블록을 넣습니다. 항목(변수) 하나마다 **다섯 가지**를 적습니다.

| 필수 항목 | 내용                                           |
| --------- | ---------------------------------------------- |
| **뜻**    | 이 값이 무엇을 의미하는가 (한국어 서술)        |
| **산식**  | 어떻게 계산했는가. 원자료면 `원자료(집계)`     |
| **범위**  | 가능한 최솟값~최댓값. 비율이면 `0~1` 처럼 명시 |
| **단위**  | 건 · 명 · % · 점 · 코드 등                     |
| **예시**  | **실제 데이터 1건**을 들어 해석까지 문장으로   |

**예시는 반드시 실제 값이어야 합니다.** 가상의 값을 쓰지 않습니다.

```
✅  "lock": 강화군 0.801 = 문화관광 146건 중 영문 등재는 29건뿐 → 80.1%가 한국어로만 열림
❌  "lock": 0~1 사이의 잠금 비율
```

또한 파일 단위로 **생성일 · 생성 스크립트 · 입력 원천 · 정렬 기준 · 행 수**를 적습니다(기획서 **P-T05** — 모든 수치에 산출일 병기).

### JSON 형식

최상위를 **객체**로 하고 `_설명`을 첫 키, 실제 행 배열은 `데이터` 키에 둡니다.

```json
{
  "_설명": {
    "파일": "89_seed_pre.json",
    "무엇": "인구감소지역 89곳의 예비 시드 랭킹",
    "생성일": "2026-09-07",
    "생성": "python data/AI/analyze_89_culture.py",
    "입력": "dump.json (2026-09-03 실측) · malgil_89_join.csv",
    "정렬": "pre 내림차순",
    "행수": 89,
    "항목": {
      "lock": {
        "뜻": "한국어로만 잠긴 비율",
        "산식": "1 − engc / cul",
        "범위": "0~1 (1에 가까울수록 한국어 전용)",
        "단위": "비율",
        "예시": "강화군 0.801 — 문화관광 146건 중 영문은 29건뿐"
      }
    }
  },
  "데이터": [{ "code": "47170", "...": "..." }]
}
```

**읽는 쪽은 하위호환으로 씁니다** — `데이터` 키가 있으면 그것을, 없으면 최상위 배열을 그대로 취급합니다.

```python
raw = json.load(open(path, encoding="utf-8"))
rows = raw["데이터"] if isinstance(raw, dict) else raw
```

### CSV 형식

`#`로 시작하는 주석 줄을 헤더 **위**에 둡니다. 읽는 쪽은 `#` 줄을 건너뜁니다.

```csv
# 파일: malgil_89_join.csv | 생성일: 2026-09-03 | 입력: dump.json · age_sgg_utf8.csv
# 정렬: 65세이상비율% 내림차순 | 행수: 89
# 행정표준코드5 : 행정표준코드 5자리. 예) 27720 = 대구 군위군
# 영문커버율%   : 영문/국문 × 100. 0~100(%). 예) 군위군 10.0 = 70건 중 7건만 영문 등재
행정표준코드5,시도,시군구,...
```

```python
with io.open(path, encoding="utf-8-sig") as f:
    rows = list(csv.DictReader(l for l in f if not l.startswith("#")))
```
