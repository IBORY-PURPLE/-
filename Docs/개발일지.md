# 말길 개발일지

> 브랜치 하나가 끝날 때마다 항목을 하나 더합니다. 사람이 나중에 읽고 「무엇을 왜 했고, 어떻게 돌리고, 무엇이 남았는지」를 알기 위한 기록입니다. 최신 항목이 위에 옵니다.
>
> 흐름: `main`에서 브랜치 → 작업 → 이 파일에 항목 추가 → push → PR → merge.

---

## B7 · `app/places-live` — /region 실시간 장소 목록 · /place 상세(Korean you'll use here) · 골든패스 완주 (2026-09-14)

**무엇** — 목업 `03_places.html`·`04_place.html`을 Flutter로 옮겨 골든패스(랜딩 → 급수 → 지도 → 지역 시트 → 목록 → 상세)를 끝까지 이었습니다. `screens/places_screen.dart`(`/region/:code` — 헤더에 지역명·시도·급수 칩·근거 한 줄, 「● Live · fetched hh:mm · 출처」, 유형 칩 12/14/15/39 · 대분류 칩 `lclsSystm1`, 카드 = 제목·주소·유형 라벨·KtoImage 썸네일, 필터는 **추가 호출 없이** 받은 목록 안에서만 거름, 푸터 「48 of 70 listings (shopping excluded) · areaBasedList2 · fetched …」) · `screens/place_screen.dart`(`/place/:id` — 히어로 16:9 + 저작권 캡션, 「This region: LvN」 칩(급수 배지는 이 화면에만), **Korean you'll use here** = 음식점(39)일 때만 `detailIntro2`를 한 번 더 불러 `firstmenu`·`treatmenu`를 메뉴 칩 4개 + 「Try: 「○○ 하나 주세요」」로, About(4줄 + Read more), Hours/Closed/Parking/Phone, Local companion, 「I stayed here today」, 푸터 「Fetched … · detailCommon2 + detailIntro2 · nothing stored on our server」) · `data/format.dart`(메뉴 문자열 → 칩 분해, 운영정보 순서) · 문자열은 `i18n/strings_en.dart`에 추가. `placeholder_screen.dart` 삭제. 테스트 3파일 추가(픽스처 = 대구 남구·노이식탁 실호출 표본) → 총 62건.

**왜** — 공모전 요건은 「서비스 안에서 공사 OpenAPI를 실시간으로 호출」입니다. 목록·상세는 매번 워커 `/api/*`를 거쳐 실시간으로 부르고, 응답은 화면에만 쓰고 저장하지 않습니다(SharedPreferences에는 급수·방문 날짜 두 키뿐). 상세를 여는 순간 `detailCommon2`, 음식점이면 `detailIntro2`까지 — 심사위원이 한 번 훑어도 오퍼레이션 3종 호출 이력이 남습니다.

**실행법** — `flutter test` · `flutter build web --no-web-resources-cdn` → worker `npx wrangler dev` → `http://127.0.0.1:8787/?demo=1` → 지도에서 남구 탭 → Places · live → 카드 탭. 상류 한도(오퍼레이션별 1,000/일)를 아끼려면 워커 캐시(5분)가 살아 있는 동안 새로고침하고, 끝나면 `taskkill /PID <pid> /T /F`.

**확인한 것** — analyze 0 · 테스트 62건(픽스처 48행 = 12·14·15·39 합, 쇼핑 38 없음 · 칩 필터 4종 모두 추가 호출 0 · 고산골 Type3 → contain / 남부도서관 Type1 → cover · 이미지 없는 대덕식당 → 텍스트 히어로 · 노이식탁 Korean 칩 4개 · 관광지(12)는 intro 미호출 · 404 → 「This place is no longer listed」 · 한도 429 → 배너 · 502 → Retry 후 직전 목록 유지 · GoRouter 하네스로 카드 탭 → 상세 → ← → 필터 유지된 목록) · 검증자가 **실시간 브라우저**로 5화면 확인: `/?demo=1`(141/225 · 30 of 89) · `/region/27200` 남구(실사진 썸네일 + ⓒ한국관광공사 캡션, 급수 배지 없음) · `/place/2842743` 노이식탁(히어로 contain · Korean 칩 4 · Hours·Phone · 「Fetched 22:26 · detailCommon2 + detailIntro2」) · `/region/12730` 구례(Lv5 잠김이어도 목록은 열리고 「갈 수 없음」류 문구 없음) · `/place/1`(404 화면) · 네트워크에 `apis.data.go.kr` 직접 호출 0(전부 `/api/*`) · lib 금지어 0건. 오늘 B7 실호출 합계 13회(구현 5 + 검증 8).

**도중 고친 것** — ① 카드 탭이 `context.go`라 상세의 ←가 `/map`으로 빠지던 것 → `push`(테스트로 고정). ② **공사 이미지 서버(`tong.visitkorea.or.kr`)가 CORS 헤더를 주지 않아** CanvasKit `Image.network`가 전부 회색 박스였습니다(B5·B6 빌드도 같은 상태였음). `KtoImage`에 `webHtmlElementStrategy: fallback`(`<img>` 폴백) + `http → https` 승격(표본 38건 중 24건이 http — https 배포에서 혼합 콘텐츠 차단 대상)을 넣어 해결. 콘솔에 CORS 오류 1줄이 남는 것은 폴백 전 1차 fetch라 정상. ③ 검증자: 워커가 404를 캐시하지 않아 죽은 contentId를 다시 열 때마다 `detailCommon2` 1콜씩 새던 것 → 429와 같이 60초 부정 캐시(`worker/src/index.ts`, README 병기).

**남은 것** — 한글 글꼴을 fonts.gstatic Noto Sans KR 런타임 다운로드에 의존(번들 없음) → 차단망·오프라인이면 tofu 가능, 제출 전 서브셋 번들 또는 preconnect 검토(B9) · 실기기에서 브라우저 뒤로가기가 필터 유지 목록으로 돌아오는지 손으로 1회 확인(합성 이벤트로는 위젯 테스트만 가능) · 워커 캐시 hit 동안 「fetched」 시각이 저장 시점에 멈춰 보임(정책엔 부합, 5분) · TSD §9 이미지 규칙에 CORS 실측·`<img>` 폴백·https 승격 기록(B10) · 공개 URL 골든패스는 `npx wrangler login` 후 배포하고 다시 봅니다.

---

## B6 · `app/level-map` — /map 화면: 레벨 카드 · 색칠 지도 · 89곳 레이어 · 드롭다운 · 지역 시트 (2026-09-14)

**무엇** — 목업 `02_map.html`을 Flutter로 1:1 옮겼습니다. `screens/map_screen.dart`(SegmentedButton 급수 전환 · 레벨 카드 · 시도→시군구 드롭다운 · 89곳 Switch · 범례 · SourceFooter · 폭 <840 바텀시트 / ≥840 우측 패널) · `widgets/choropleth_map.dart`(자산 경계 → `ui.Path` 1회 캐시, 상태별 칠: Lv3/4/5 파랑 3단 · Lv1~2 회색 · 잠김 흰 바탕+45° 빗금 · 제외 점선 · 보류 파선 · 89곳 굵은 테두리, `Path.contains` 역변환 히트테스트, InteractiveViewer 핀치 줌 1~4배) · `widgets/region_sheet.dart`(이름·시도 · 급수 칩 · 89 배지 · 근거 한 줄 · 산출일·출처 · 잠김 배너 · 「Places · live」 → `/region/:code` · Local companion(kctg.or.kr 새 탭) · 「I stayed here today」 자기신고 → 기기 저장 · 한국어 각주 2줄). 테스트 3파일 추가 → 총 43건.

**왜** — 심사 시연의 핵심 화면. 레벨 카드 수치는 자산 `요약`을 그대로 읽어(재계산 금지) 데이터 리포트와 화면 숫자가 어긋날 여지를 없앴고(Lv3 = 141/225 · 89곳 중 30), 시군구 드롭다운은 PRD F3대로 **`ldongCode2` 실시간 호출**(코드 개편 자동 반영)하되 실패·한도면 자산 목록으로 조용히 폴백해 지도와 카드는 항상 뜹니다(「지도 로딩 실패」 상태 없음).

**실행법** — `flutter test` · `flutter build web --no-web-resources-cdn` → worker `npx wrangler dev` → 브라우저 `http://127.0.0.1:8787/?demo=1`. 헤드리스 스크린샷은 폭 500 이상, 종료는 `taskkill /PID <pid> /T /F`.

**확인한 것** — analyze 0 · 테스트 43건(레벨 카드 = 요약(급수 2~5) · 델타 = 누적 차이 · 칠 규칙 · 구례군 내부점 히트 → 12730, 일반구 → parent · 잠긴 구례(사용자 Lv3)에 「Opens at Lv5 — you can still visit…」·금지어 없음 · API 502여도 에러 배너 없음 · 휠 스크롤이 지도를 확대하지 않음) · 빌드 · 워커 서빙 스크린샷(500·1100)에서 한반도 색칠·카드 「Open now 141 of 225 · Depopulation areas: 30 of 89 · At Lv4: +55 — 순천시 · 나주시 · 담양군 · 화순군 …」 · 워커 로그 `/api/ldong 200`(앱이 런타임 호출).

**검증자가 고친 것** — 마우스 휠이 페이지 스크롤 중 지도를 저절로 확대하던 것(`scaleFactor: double.infinity` — 핀치·Ctrl+휠 줌은 유지) + 회귀 테스트.

**남은 것** — `/region/:code`·`/place/:id`(B7) · 시트는 위젯 테스트로만 확인(스크린샷은 탭 필요) · 세종 시도 선택 시 `regn=36` 실호출 결과 미검증(0건이면 자산 폴백) · 모바일 실기기에서 리스트 안 핀치 줌 동작 확인 · 줌 리셋(더블탭) 없음.

---

## B5 · `app/flutter-shell` — Flutter Web 셸: 토큰·라우터·상태·자산·API·로딩 셸·랜딩 (2026-09-14)

**무엇** — `app/flutter/lib/`에 앱 뼈대를 만들었습니다. `main.dart`(`usePathUrlStrategy()` → 자산 로드 + 상태 복원 → runApp) · `app.dart`(MaterialApp.router · GoRouter `/` `/map` `/region/:code` `/place/:id` · `?demo=1`이면 Lv3 저장 후 `/map`) · `theme/tokens.dart`(목업 `m3.css`와 hex 1:1 — 검증자가 37개 전수 대조) · `state/app_state.dart`(급수·방문 기록, shared_preferences, 실패 시 메모리) · `data/assets.dart`(자산 JSON 파서 — `데이터` 키 하위호환, Region·Meta·Summary·경계, stateOf/isOpen) · `data/api.dart`(ApiClient — `API_BASE` dart-define, 빈 값이면 같은 오리진 `/api/*`, ok/quota/error 3분기) · `i18n/strings_en.dart`(UI 문자열 1파일) · `widgets/`(kto_image — 앱 유일 이미지 위젯, state_views, source_footer, level_chip) · `screens/landing_screen.dart`(목업 01 그대로) · `screens/placeholder_screen.dart`(지도·지역·장소는 다음 브랜치). `web/index.html`에 **정적 로딩 셸**(엔진 부팅 전 스켈레톤, `flutter-first-frame`에서 제거). 테스트 24건. 워커 `wrangler.toml [assets]`를 `../flutter/build/web`으로 교체.

**왜** — 이후 화면이 같은 테마·상태·API 클라이언트를 쓰게 하려고. 열린 곳수는 앱이 재계산하지 않고 자산 `요약` 값을 그대로 씁니다(P-T05) — 대신 테스트에서 지역 행 재계산 == 요약을 급수 2~5 전부 검사합니다.

**실행법**
```
(worker)  npm run dev                                   # 8787
(flutter) flutter run -d chrome --dart-define=API_BASE=http://127.0.0.1:8787
빌드·서빙: flutter build web --no-web-resources-cdn → (worker) npx wrangler dev   # / 에 Flutter, /api/* 프록시
```
Flutter 빌드가 없으면 `wrangler dev`가 assets 디렉터리 오류를 냅니다(먼저 빌드).

**확인한 것** — `flutter analyze` 0 · `flutter test` 24건 통과 · `flutter build web --no-web-resources-cdn` 성공(CanvasKit 셀프호스팅) · 워커 경유 `/` 로딩 셸 · `/api/health` 200 · `/map` SPA 폴백 · `?demo=1` → `/map` Lv3 · 헤드리스 Edge 스크린샷에 급수 카드 4장·버튼 2개·산출일 푸터 · lib/ 금지어 0건 · `apis.data.go.kr` 직접 호출 0건 · serviceKey 0건.

**도중 고친 것** — Flutter Web 기본 URL 전략이 해시(`#/`)라 `?demo=1`·`/map`이 라우터에 닿지 않던 것을 `usePathUrlStrategy()`로 해결. 헤드리스 Edge는 폭 430을 줘도 492로 보고하므로 스크린샷은 500 이상으로.

**남은 것** — `/map`(choropleth CustomPaint · 레벨 카드 · 지역 시트 · 드롭다운) → B6, `/region`·`/place`(실시간 목록·상세) → B7. 위젯 테스트(KtoImage·StateViews)와 실기기 첫 페인트 측정은 아직. `build/web`은 커밋하지 않으므로 배포 전 로컬에서 빌드.

---

## B4 · `docs/mockup` — HTML 목업 (Flutter Web · Material 3 예상 화면) (2026-09-14)

**무엇** — `Docs/mockup/`에 화면 5개 + 안내 index를 순수 HTML/CSS/JS로 만들었습니다. 외부 CDN 없이 **파일을 더블클릭(file://)하면 열립니다.** `index.html`(화면 링크 · Flutter 위젯 대응표 · 토큰표 · 문구 규칙) · `01_landing.html`(급수 카드 4장 · Judge preview) · `02_map.html`(레벨 카드 · 시군구 색칠 지도 · 89곳 토글 · 시도/시군구 드롭다운 · 지역 시트) · `03_places.html`(유형 칩 · 대분류 칩 · 이미지 규칙) · `04_place.html`(Korean you'll use here · 말벗 · 방문 기록) · `05_states.html`(로딩 · 빈 필터 · 오류+Retry · 일일 한도 · 잠긴 지역 · 제외 · 보류 · Any level · 이미지 규칙). 공용: `m3.css`(Material 3 토큰 — 변수명 = Flutter ColorScheme 역할명) · `mockup.js`(급수 상태 · 열림 판정 · 지도 렌더 · 시트) · `data.js`(B3 산출, 앱과 같은 자산) · `fixtures/`(대구 남구 실호출 표본 2026-09-14 · 앱은 읽지 않음).

**왜** — Flutter를 짜기 전에 정보 위계·문구·상태 화면을 눈으로 확정하려고. 목업의 CSS 변수명·색 hex·컴포넌트를 Flutter 위젯과 1:1로 두어(각 요소에 `data-flutter="FilledButton"` 식으로 위젯명 기입) 개발할 때 그대로 옮깁니다.

**디자인 결정** — seed `#1F4E79`(개요서 남색) 기준 M3 라이트 스킴. 지도는 dataviz 규칙대로 **파랑 한 색 순차 램프**(Lv3 `#86b6ef` · Lv4 `#2a78d6` · Lv5 `#104281`, `--ordinal` 검증 통과) + Lv1~2 회색 「Any level」 + 잠김은 45° 빗금 + 제외 점선 + 보류 파선 + 89곳 굵은 외곽선 — 색뿐 아니라 패턴으로도 구분(색각 대응). UI 영어 · 콘텐츠(지명·장소명·메뉴·overview) 한국어 원문. 수치는 개수와 「1,000명 중 N명」「10명 중 N명」만.

**확인한 것** — 헤드리스 Edge 스크린샷 6장(랜딩·지도·목록·상세·상태·index) 정상 렌더. 지도는 `data.js`로 Lv3 기준 「Open now 141 of 225 · Depopulation areas 30 of 89」와 색칠 확인. 금지어 grep(`TourAPI|KTO|Korea Tourism|Lv0|Lv6|갈 수 없|can't go|legal criteria|한국의 N%`) — 규칙을 설명하는 index 문장 2건 외 0건.

**보는 법** — `Docs/mockup/index.html` 더블클릭 → 화면 순서대로. 우상단 「Desktop width」로 넓은 폭 확인. `02_map.html?demo=1`은 Lv3 프리셋. **사용자 검토 대기 — 색·문구·구성 피드백을 받은 뒤 Flutter 화면을 그대로 옮깁니다.**

**남은 것** — 목업의 급수 요약(열린 곳수)은 `mockup.js`가 자산에서 다시 세는 값이고, 앱은 자산의 `요약` 섹션 값을 그대로 씁니다(같아야 함 — 검증 항목). 폰트는 시스템 스택(Pretendard 셀프호스팅은 미정).

---

## B3 · `data/boundary-assets` — 앱 자산 빌더 `build_app_assets.py` + Flutter 스캐폴드 (2026-09-14)

**무엇** — `data/AI/build_app_assets.py`(신규)가 급수 판정 결과(`인간/analyze_topik_local.json` 230곳) · 공사 시군구 목록(`_raw/ldong_269.json`) · 인구감소지역 89곳 · 시군구 경계(`_raw/sgg_20260701_light.parquet` 256행, WKB WGS84)를 **한 파일** `app/flutter/assets/data/build_app_assets.json`(376.5KB)로 묶습니다. `--mockup`이면 같은 내용을 `Docs/mockup/data.js`(`window.MALGIL = …;`)로도 씁니다. 이 브랜치에 **Flutter 프로젝트 뼈대**(`flutter create --empty --platforms web`, 패키지 http · go_router · shared_preferences · url_launcher, `pubspec.yaml` assets 등록)도 함께 올렸습니다 — 자산 파일이 그 안에 살기 때문입니다. 화면 코드는 아직 없습니다(B5).

**섹션** — `메타`(입력별 산출일 · 경계 출처·라이선스 · 투영식 · viewBox · 단순화 허용오차 · 규칙 상수 · 곳수) · `급수규칙`(감도표 포함) · `요약`(급수별 곳수 · 사용자 급수별 누적 열린 곳) · `지역`(269행 — 일반구 39행은 parent 시의 값을 복사하고 `parent` 기록) · `경계`(`{code: [[ [x,y],… ], …]}` 정수 좌표 링).

**왜** — 앱과 목업이 같은 파일에서 같은 수치를 읽어야 산출일·곳수가 어긋나지 않습니다(P-T05). 경계는 shapely·pyproj 없이 WKB를 `struct`로 직접 읽어 의존성을 pyarrow·numpy로 묶었고, 모바일에서 바로 그리도록 viewBox `0 0 1000 1300` 정수 좌표로 미리 투영했습니다(x = (lon−lon0)·cos36°, y = lat0−lat, 1px ≈ 0.66km, Douglas-Peucker 0.5px).

**실행법**
```
python data/AI/build_app_assets.py --mockup   # 앱 자산 + Docs/mockup/data.js (--eps 1.0 이면 245.9KB)
cd C:\Users\chewo\StudioProjects\malgil\flutter && flutter build web --no-web-resources-cdn
```
stdout에 코드 집합 대조(경계 256 vs ldong 269 vs 급수후보 230 vs 89곳)를 찍고, 89곳에 경계가 없거나 요약 재계산이 원본과 다르거나 1MB를 넘으면 exit 1.

**확인한 것 (검증자 독립 검사)** — 지역 269행 = ldong 269 · code 유일 · 89곳 89/89(lv 원본과 동일 · 경계 직접 존재) · 기초 230행의 11개 필드 = 급수후보 · 일반구 parent 39건 정합 · 요약 = 원본(84/57/55/29/1/4) · 좌표 전부 정수·범위 안 · 링 933(면적 전부 양수) · 위경도 방향 정상(종로 y < 제주 y, 인천 x < 강릉 x, 독도 x=999) · parquet 면적 순위 상위 5와 신발끈 면적 순위 일치(r=0.9998) · `data.js` == JSON · `flutter build web` 성공(35초) · 목업 `02_map.html?demo=1` 렌더 확인(Lv3: 열린 곳 141/225 · 89곳 중 30).

**남은 것** — 일반구를 둔 시 13곳(수원·성남·안양·부천·안산·고양·용인·화성·청주·천안·포항·창원·전주)은 경계가 구 단위뿐이라 `지역.parent`로 잇습니다. 1px 이하로 줄어든 섬 140개는 2px 점 링으로만 보입니다(확대 지도가 필요하면 별도 자산). 급수 산출물이 바뀌면 이 스크립트를 다시 돌립니다 — `data.js`·앱 자산은 손으로 고치지 않습니다.

---

## B1 · `infra/worker-proxy` — Cloudflare Workers 프록시 (app/worker) (2026-09-14)

**무엇** — `app/worker`(실경로 `C:\Users\chewo\StudioProjects\malgil\worker`)에 단일 워커를 만들었습니다: 정적 자산(`/*`, 지금은 임시 `public/index.html`) + API 프록시(`/api/*`) + cron(매일 00:00 UTC = KST 09:00). Hono 4.13 · wrangler 4.131 · TypeScript. 파일: `wrangler.toml` · `src/index.ts`(라우트·3층 캐시·CORS·레이트리밋·cron) · `src/kto.ts`(공통 fetch + parseKto 분기 + 세종 특례) · `src/regions89.ts`(89곳 코드) · `public/index.html` · `README.md`. `.dev.vars`(로컬 키)는 커밋하지 않습니다.

**왜** — 인증키를 클라이언트에 내리지 않고(TSD §8-4) Flutter Web이 실시간 호출하게 하고, 심사 규정의 「개발 기간 내 매일 실시간 호출 이력」을 cron으로 자동 확보합니다. 클라이언트는 화이트리스트된 경로·파라미터만 쓸 수 있고 그 밖은 400입니다.

**엔드포인트 ↔ 공사 오퍼레이션 (KorService2 · 기능설명서 4장 원본)**

| 말길 엔드포인트 | 오퍼레이션 | 넘기는 파라미터 | 캐시 | 용도 |
|---|---|---|---|---|
| `GET /api/health` | — | — | — | 상태·바인딩 확인 |
| `GET /api/ldong[?regn=NN]` | `ldongCode2` | numOfRows=1000 pageNo=1 [lDongRegnCd] | 3600s | 시군구 드롭다운 · cron |
| `GET /api/places?code=NNNNN[&type=12\|14\|15\|39]` | `areaBasedList2` | lDongRegnCd(2)+lDongSignguCd(3) · numOfRows=1000(고정) · arrange=C [contentTypeId] | 300s | 장소 목록 · cron |
| `GET /api/place/:id` | `detailCommon2` | contentId | 300s | 상세 |
| `GET /api/place/:id/intro?type=NN` | `detailIntro2` | contentId · contentTypeId | 300s | 음식점 실무 필드 |

공통 파라미터 MobileOS=ETC · MobileApp=malgil · _type=json. 응답 `{ok, fetchedAt, remaining, totalCount, items}` + 헤더 `x-malgil-remaining` · `x-malgil-cache` · `x-malgil-cache-layer`. 상류 한도 초과(XML) → 429 `{kind:'quota'}`, 존재하지 않는 contentId → 404, IP 분당 120 초과 → 429 `{kind:'rate_limited'}`.

**실행법**
```
cd C:\Users\chewo\StudioProjects\malgil\worker
npm run typecheck · npm run dev (포트 8787, .dev.vars 의 KTO_KEY) · curl http://127.0.0.1:8787/cdn-cgi/local/scheduled (cron 수동)
배포: npx wrangler login → npx wrangler secret put KTO_KEY → npx wrangler kv namespace create MALGIL_CACHE (id를 wrangler.toml에) → npm run deploy
```

**확인한 것 (2026-09-14 · wrangler dev 실호출)** — `/api/ldong` 16건 · `?regn=30` 5건 · `places?code=30140&type=39` 39건 · `12730`(구례) 142/106 · **`36110`(세종) 204/169 — `lDongRegnCd=36&lDongSignguCd=110`은 0건이라 `36110/36110` 특례로 구현** · `28125`(제물포구) 189/162 · `place/1807489` 광천식당 · `intro?type=39` firstmenu=두부두루치기. 잘못된 파라미터·중복 키·type=38·미정의 경로 → 400/404. 같은 요청 2회째 hit. CORS는 localhost·127.0.0.1만 허용. 상류 잔여가 오퍼레이션마다 달라 **한도 1,000/일은 오퍼레이션별**임을 확인. 키 앞 8자 grep → `.dev.vars` 뿐. 검증자가 고친 것: 같은 쿼리 키 중복으로 캐시를 우회하던 구멍 · 잔여 0인 성공 호출을 429로 버리던 것 · 존재하지 않는 contentId 404 · overview 본문에 'errMsg'가 섞이면 XML 오류로 오판하던 것 · health에 keyConfigured.

**검증 뒤 추가한 것** — ① **3층 캐시**(메모리 → KV → Cache API): `*.workers.dev`에서는 Cache API가 동작하지 않아(커스텀 도메인 전용) KV가 실질 캐시가 됩니다 → 배포 후 KV 생성 필수. 429는 메모리에 60초 부정 캐시. ② **레이트리밋 바인딩**(`[[ratelimits]]` IP별 분당 120) — 워커 URL로 공사 한도를 태우는 남용 방지. ③ 공개 `rows` 파라미터 제거(값을 바꿔 캐시 키를 달리하던 구멍).

**남은 것** — 배포는 사용자의 `npx wrangler login` 뒤에(비대화형 환경에서는 CLOUDFLARE_API_TOKEN 필요). 배포 후 `/api/health`에서 `keyConfigured·cache.kv·rateLimit` 셋 다 true인지, 같은 URL 2회 호출로 hit인지, `npm run tail`로 cron 로그가 찍히는지 확인. Flutter 빌드가 나오면 `[assets] directory`를 `../flutter/build/web`으로. 실제 한도 초과(429) 경로는 실호출로 만들 수 없어 단위 실행으로만 검증했습니다.

---

## B2 · `data/level-rule-v2` — 급수 규칙 v2 · 기초지자체 집합 230곳 고정 · 인천 개편 반영 (2026-09-14)

**무엇** — 급수 판정을 백분위(ps≥60) 기준에서 **절대값 상수 4개(KOR_T 20 · SHARE_T 2.0% · OLD65_T 40% · PB_T 60)** 로 바꾸고, 기초지자체 집합을 공사 `ldongCode2` 실측(시군구 269 → 일반구 39곳을 시로 접은 **230곳**)으로 고정했습니다. 신규 `pull_ldong.py` → `_raw/ldong_269.json`, `dump.py` 재덤프(9/14 · 국문 49,722건 · 영문은 `analyze_89_culture.py`용으로만 유지), `_raw/code_bridge.json`을 `_설명·매핑(27쌍)·그룹(인천 2묶음)` 구조로 확장, `analyze_visitor_gap.py` → `analyze_topik_local.py` → `analyze_89_hypothesis.py` 세 스크립트를 고쳐 재실행했습니다. `pull_visitor.py`의 `.env` 경로 버그(`data/.env`를 찾던 것)도 고쳤습니다.

**왜** — ① `analyze_topik_local.py`가 광역시 코드면 전부 「자치구」로 잡아 군위·달성·강화·옹진·기장·울주가 자치구로 오분류됐고, 하류 `analyze_89_hypothesis.py`가 그것을 되돌리는 보정을 안고 있었습니다. ② 인천 2026-07 개편(중구+동구 → 제물포구+영종구 · 서구 → 서해구+검단구)을 신→구 브릿지로 억지로 맞추고 있었습니다. ③ 방문자 원장에는 시 행(41110 수원시)과 일반구 행(41111~)이 둘 다 있고 시 행 ≈ 일반구 합인데 더해서 이중 계상했습니다. ④ 코드 규칙만으로 일반구를 찾으면 증평군 43745가 영동군 43740에 흡수됐습니다(이름 조건으로 해결). ⑤ 근거 문장이 백분위로 쓰여 있어 「N%」 금지와 충돌했습니다.

**규칙 v2** — `months<12 → 보류 / kor<20 → 제외 / share≥2.0 → Lv1~2 / 자치구 or pb≥60 → Lv3 / 군 and e65≥40 → Lv5 / 나머지 Lv4`. 구분은 이름 접미사로만(군 · 구=자치구 · 시). 근거 문장은 절대값만 — 「방문자 1,000명 중 외국인 4명 · 군 · 주민 10명 중 4명이 65세 이상 · 국문 관광자원 106건」 + 영문 병기.

**실행법** (순서 고정 · `data/AI`에서)
```
python pull_ldong.py            # 17콜 → _raw/ldong_269.json
python dump.py                  # 66콜 → _raw/dump.json (.env Api_Key_Decoding)
python analyze_visitor_gap.py   # → 인간/analyze_visitor_gap.json (230행)
python analyze_topik_local.py   # → 인간/analyze_topik_local.json
python analyze_89_hypothesis.py # → 인간/analyze_89_hypothesis.json
```
방문자 원장(`c1_visitor_raw.json`, 2025-08~2026-07)은 재수집하지 않았습니다(8월분 미완).

**결과 (2026-09-14 산출)** — 전체 230 / 89곳 89: **Lv1~2 84/28 · Lv3 57/2 · Lv4 55/30 · Lv5 29/29 · 제외 1/0 · 보류 4/0**. 사용자 급수별 누적 열린 곳(전체/89): Lv1~2 84/28 · Lv3 141/30 · Lv4 196/60 · Lv5 225/89. 감도: OLD65_T 35/38/40/42/45 → Lv5 41/34/29/20/8곳 · SHARE_T 1.5/2.0/2.5/3.0 → Lv1~2 113/84/60/49곳(89곳 42/28/19/16). **Lv5 29곳은 전부 인구감소지역.**

> ⛔ **이전 수치는 폐기합니다** — 「264곳 · 101/68/76/19」와 「227곳 · 91/57/78/1」(9/10 판)은 더 이상 인용하지 않습니다. 「Lv4~5」 칸도 없어졌습니다.

**이동한 89곳** — Lv4~5 → Lv4 30곳(담양·화순·장성·순창·영월·옥천·태백·연천·제천·고성·공주·문경 등) · Lv4~5 → Lv5 25곳(구례·임실·곡성·함양·단양·강진·합천·무주·청도·부여·장흥·보은 등) · **Lv1~2 → Lv5 3곳: 고창(1.875%)·청송(1.891%)·진안(1.912%)** — 옛 백분위 60 기준으로는 Lv1~2였으나 절대 문턱 2.0% 아래 · 군 · e65≥40 · **Lv3 → Lv5: 대구 군위군**(오분류 해소, e65 50.4%). 달성군은 Lv3 → Lv4.

**확인한 것 (검증자 독립 재계산)** — 230행에 규칙 v2를 다시 적용해 lv 불일치 0 · 세 산출물 곳수·요약 상호 일치 · 감도표 9행 일치 · e65를 원본 CSV에서 직접 계산해 구례 41.63 · 군위 50.36 · 대구 서구 30.08(±0.05) · 구코드(28110/28140/28260/29xxx/46xxx) 0건 · 절대 인원수 필드 0건 · 인천 신설 4구 `보류`(months=1) · 군위·달성 「군」 · 89곳 합 89 · `_설명` 5요소 전부 채움(검증자가 누락 2건 보완: 말길구분 단위 · 요약 정렬/행수) · 예시 8건이 실제 값과 일치.

**남은 것**
- 9/7 실측 리포트의 「89곳 share 중앙값 1.43 vs 비89곳 1.41 (89곳이 오히려 높음)」 문장은 230곳 집합에서 **1.434 vs 1.565(89곳이 낮음)** 로 뒤집혔습니다. 결론(「지정 여부로는 갈리지 않는다」)은 유지되나 `Docs/plan/실측_방문자격차_2026-09-07.md` · 기획서 §2-3~§2-4 수치는 `docs/submission`에서 갱신합니다.
- 인천 서구 28260의 202607 외국인 행 5건(7/1~7/5)은 「중복」으로 지웠지만 엄밀히는 서해구 행(7/6~)으로 이어지는 코드 전환입니다. 영향은 참고값 5일치뿐이며 `_설명`에 적어 두었습니다.
- 8월 완전월이 나오면 `pull_visitor.py` 재수집 → 인천 4구 months 누적(12개월이 차면 보류 해제).
- `analyze_89_culture.py`는 재실행하지 않았습니다(9/14 덤프로 돌리면 건수가 소폭 바뀜).

---

## B0 · `chore/repo-sync` — 9/14 기준 문서·데이터 세대교체분 일괄 반영 (2026-09-14)

**무엇** — 9/3 이후 로컬에만 있던 것들을 저장소에 올렸습니다. 기획서 v0.8(현행 정본) · 문제정의 v1 · 모두의창업 지원서 · `Docs/plan/` 실측 리포트 5건 · `data/AI/`(스크립트 + `_raw/`) · `data/인간/`(자기설명 JSON 4개) · `CLAUDE.md`. 구 `data/` 직하 스크립트는 `data/AI/`로 옮겨진 것이라 삭제로 잡힙니다.

**왜** — 앱 개발 브랜치의 기준점이 필요했고, 원격 `main`이 9/3(커밋 2개)에 멈춰 있어 디스크 사고 시 9월 작업이 통째로 사라질 상태였습니다.

**함께 바꾼 것**
- `.gitignore` — `*.parquet`(경계 원본, 재다운로드 가능) · `app/` 빌드 산출물·`node_modules` · `default/`(주최측 배포 자료) · `_workspace/`(개인 작업 로그) · `topik30.html`(TOPIK 사이트 캡처). 저장소가 **public**이라 주최측 자료와 개인 로그는 올리지 않습니다.
- `CLAUDE.md` — `build_*.py` → 앱 자산 규칙 1행, `app/` 정션 설명, **`git clean -d` 금지**.
- `README.md` — 저장소 구조·실행 방법을 현재 구조(`data/AI` · `data/인간` · `app/`)로 다시 썼습니다. 영문 커버율 서사(P-M04로 폐기)를 뺐습니다.

**확인한 것** — `git ls-files`에 `.env` · `dump.json` · `c1_visitor_raw.json` · `*.parquet` 이 없음.

**남은 것** — 분석 로직은 이 커밋에서 바꾸지 않았습니다. 급수 규칙 v2·인천 코드 개편 반영은 `data/level-rule-v2` 브랜치에서 합니다. `data/AI/join.py`는 4행에서 `os`·`RAW`를 정의 전에 써서 실행이 안 됩니다(앱 파이프라인은 이 파일을 쓰지 않음).
